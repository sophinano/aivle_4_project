spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/aivleDB
    username: admin
    password: admin123
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
openai:
  api:
    key: {api-key}
